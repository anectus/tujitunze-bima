Frontend scaffold (Next.js). App routes: member/dashboard, member/wallet, member/transactions, admin/dashboard, admin/reconciliation. Use server components for data fetching where appropriate. Follow agent rules in node_modules/next/dist/docs/ when implementing; do not assume standard Next.js APIs.

Security: prefer httpOnly cookie auth for production; keep JWT usage minimal for MVP demos.